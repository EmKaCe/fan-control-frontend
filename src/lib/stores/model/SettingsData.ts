export interface SettingsData {
    url: string,
    zip: number,
    pollingRate: number,
    nightMode: boolean,
    nightModeStart: string,
    nightModeEnd: string,
    nightModeSpeed: number,
    ignoreWindow: boolean,
}